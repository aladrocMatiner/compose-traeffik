# cloud-init Overrides (Gentoo Experimental)

Only add Gentoo-specific templates/overrides here if the shared templates in `infra/cloud-init/` are not sufficient.

Keep a note in `docs/decision-log.md` explaining why each override exists.
