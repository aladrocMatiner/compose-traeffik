# DNS Service Data

This directory stores persistent data for the DNS service (Technitium).

- `dns/data/` is mounted to `/etc/dns` inside the container.
- Do not commit real credentials or secrets here.
