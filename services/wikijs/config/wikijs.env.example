# Fallback env file so compose config parsing works before wikijs-bootstrap.
# scripts/wikijs-render-config.sh overwrites services/wikijs/rendered/wikijs.env with real values.
WIKIJS_RENDER_STATUS=placeholder
PORT=3000
DB_TYPE=postgres
DB_HOST=wikijs-db
DB_PORT=5432
DB_USER=wikijs
DB_PASS=placeholder
DB_NAME=wiki
HA_ACTIVE=false
NODE_EXTRA_CA_CERTS=
